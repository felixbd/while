// init var state
rv := 0;
a := 420;
b := 69;

// loop will be exec b-times
while b != 0 do

    // calculate rv += a
    //  -> a * b = a + a + ... (b-times)

    temp := rv;
    loop a do temp := temp + 1 end;
    rv := temp;

    b := b - 1
end
